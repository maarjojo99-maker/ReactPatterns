# Advanced React Patterns

A project exploring sophisticated architectural patterns in React.

## Features
- Compound components.
- Render props.
- High-order components (HOCs).
- Custom hooks for shared logic.
- Performance optimization with `React.memo` and `useMemo`.

## Getting Started
1. Clone the repository.
2. Run `npm install`.
3. Run `npm run dev`.
